<div class="col-12">
  <h3 class="mb-5">商品一覧<span style="font-size: 18px;">（全<%= @products.count %>件）</span></h3>
  <ul class="product-list d-flex flex-wrap">
    <% @products.each do |product| %>
      <li class="product-list-item">
        <%= link_to product_path(product) do %>
          <%= attachment_image_tag product, :product_image, size: "300x200", fallback: "no_image.jpg" %>
          <p class="mt-3"><%= product.name %></p>
          <p>¥<%= include_tax(product.price).to_s(:delimited) %>（税込）</p>
        <% end %>
      </li>
    <% end %>
  </ul>
  <%= paginate @products %>
</div>
